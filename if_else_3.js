// Take a user's age and check if the user is eligible to vote (age ≥ 18).

let age = 24;

if (age >= 18) {
    console.log("you are eligible, you can vote");
} else {
    console.log("you are not eligible, you can't vote");
}
